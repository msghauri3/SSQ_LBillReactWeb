name: Deploy React Website to GCP

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build React application
        run: npm run build

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VM_SSH_KEY }}" > ~/.ssh/gcp_key
          chmod 600 ~/.ssh/gcp_key
          ssh-keyscan -H "${{ secrets.VM_HOST }}" >> ~/.ssh/known_hosts

      - name: Test SSH connection
        run: |
          ssh -i ~/.ssh/gcp_key \
            ${{ secrets.VM_USER }}@${{ secrets.VM_HOST }} \
            "echo SSH connection successful"

      - name: Prepare deployment folder
        run: |
          ssh -i ~/.ssh/gcp_key \
            ${{ secrets.VM_USER }}@${{ secrets.VM_HOST }} \
            "rm -rf /tmp/lbill_web_react_deploy && mkdir -p /tmp/lbill_web_react_deploy"

      - name: Upload React build
        run: |
          scp -i ~/.ssh/gcp_key \
            -r ./build/. \
            ${{ secrets.VM_USER }}@${{ secrets.VM_HOST }}:/tmp/lbill_web_react_deploy/

      - name: Deploy React files
        run: |
          ssh -i ~/.ssh/gcp_key \
            ${{ secrets.VM_USER }}@${{ secrets.VM_HOST }} \
            "cp -rf /tmp/lbill_web_react_deploy/. /var/www/lbill_web_react/"

      - name: Cleanup temporary files
        run: |
          ssh -i ~/.ssh/gcp_key \
            ${{ secrets.VM_USER }}@${{ secrets.VM_HOST }} \
            "rm -rf /tmp/lbill_web_react_deploy"

      - name: Verify deployment
        run: |
          ssh -i ~/.ssh/gcp_key \
            ${{ secrets.VM_USER }}@${{ secrets.VM_HOST }} \
            "test -f /var/www/lbill_web_react/index.html && echo 'Deployment successful'"
